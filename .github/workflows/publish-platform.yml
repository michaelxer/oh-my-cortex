name: publish-platform
run-name: "platform packages ${{ inputs.version }}"

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      dist_tag:
        required: false
        type: string
        default: ""
      platform:
        required: false
        type: string
        default: ""
  workflow_dispatch:
    inputs:
      version:
        description: "Version to publish (e.g., 0.1.1-beta.1)"
        required: true
        type: string
      dist_tag:
        description: "npm dist tag (e.g., beta, latest)"
        required: false
        type: string
        default: ""
      platform:
        description: "Single platform to publish; leave blank for all"
        required: false
        type: string
        default: ""

permissions:
  contents: read
  id-token: write

jobs:
  build:
    runs-on: ${{ startsWith(matrix.platform, 'windows-') && 'windows-latest' || startsWith(matrix.platform, 'darwin-') && 'macos-latest' || 'ubuntu-latest' }}
    defaults:
      run:
        shell: bash
    strategy:
      fail-fast: false
      max-parallel: 11
      matrix:
        platform: [darwin-arm64, darwin-x64, darwin-x64-baseline, linux-x64, linux-x64-baseline, linux-arm64, linux-x64-musl, linux-x64-musl-baseline, linux-arm64-musl, windows-x64, windows-x64-baseline]
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install
        env:
          BUN_INSTALL_ALLOW_SCRIPTS: "@ast-grep/napi"

      - name: Validate release inputs
        id: validate
        env:
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_DIST_TAG: ${{ inputs.dist_tag }}
          INPUT_PLATFORM: ${{ inputs.platform }}
        run: |
          VERSION="$INPUT_VERSION"
          DIST_TAG="$INPUT_DIST_TAG"
          PLATFORM="$INPUT_PLATFORM"

          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z]+(\.[0-9A-Za-z]+)*)?$ ]]; then
            echo "::error::Invalid version: $VERSION"
            exit 1
          fi

          if [ -n "$DIST_TAG" ] && ! [[ "$DIST_TAG" =~ ^[a-z][a-z0-9-]*$ ]]; then
            echo "::error::Invalid dist_tag: $DIST_TAG"
            exit 1
          fi

          case "$PLATFORM" in
            ""|darwin-arm64|darwin-x64|darwin-x64-baseline|linux-x64|linux-x64-baseline|linux-arm64|linux-x64-musl|linux-x64-musl-baseline|linux-arm64-musl|windows-x64|windows-x64-baseline) ;;
            *)
              echo "::error::Invalid platform: $PLATFORM"
              exit 1
              ;;
          esac

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "dist_tag=$DIST_TAG" >> $GITHUB_OUTPUT
          echo "platform=$PLATFORM" >> $GITHUB_OUTPUT

      - name: Check if already published
        id: check
        env:
          VERSION: ${{ steps.validate.outputs.version }}
          TARGET_PLATFORM: ${{ steps.validate.outputs.platform }}
        run: |
          if [ -n "$TARGET_PLATFORM" ] && [ "$TARGET_PLATFORM" != "${{ matrix.platform }}" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Skipping oh-my-cortex-${{ matrix.platform }} because target platform is ${TARGET_PLATFORM}"
            exit 0
          fi

          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://registry.npmjs.org/oh-my-cortex-${{ matrix.platform }}/${VERSION}")

          if [ "$STATUS" = "200" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "oh-my-cortex-${{ matrix.platform }}@${VERSION} already published"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
            echo "oh-my-cortex-${{ matrix.platform }}@${VERSION} needs publishing"
          fi

      - name: Update version in package.json
        if: steps.check.outputs.skip != 'true'
        env:
          VERSION: ${{ steps.validate.outputs.version }}
        run: |
          cd packages/${{ matrix.platform }}
          jq --arg v "$VERSION" '.version = $v' package.json > tmp.json
          mv tmp.json package.json

      - name: Set root package version
        if: steps.check.outputs.skip != 'true'
        env:
          VERSION: ${{ steps.validate.outputs.version }}
        run: |
          jq --arg v "$VERSION" '.version = $v' package.json > tmp.json
          mv tmp.json package.json

      - name: Pre-download baseline compile target
        if: steps.check.outputs.skip != 'true' && endsWith(matrix.platform, '-baseline')
        run: |
          BUN_VERSION=$(bun --version)
          PLATFORM="${{ matrix.platform }}"
          PKG_NAME="bun-${PLATFORM}"
          CACHE_DIR=$(bun pm cache)
          CACHE_DEST="${CACHE_DIR}/${PKG_NAME}-v${BUN_VERSION}"

          if [[ -f "$CACHE_DEST" ]]; then
            echo "Compile target already cached at ${CACHE_DEST}"
            exit 0
          fi

          echo "Pre-downloading ${PKG_NAME} v${BUN_VERSION} to ${CACHE_DEST}"
          TARBALL_URL="https://registry.npmjs.org/@oven/bun-${PLATFORM}/-/bun-${PLATFORM}-${BUN_VERSION}.tgz"

          mkdir -p "$(dirname "$CACHE_DEST")"
          TMP_DIR=$(mktemp -d)

          curl -fsSL --retry 5 --retry-delay 5 "${TARBALL_URL}" | tar -xzf - -C "${TMP_DIR}"

          if [[ "$PLATFORM" == windows-* ]]; then
            BIN_NAME="bun.exe"
          else
            BIN_NAME="bun"
          fi

          if [[ -f "${TMP_DIR}/package/bin/${BIN_NAME}" ]]; then
            cp "${TMP_DIR}/package/bin/${BIN_NAME}" "${CACHE_DEST}"
          elif [[ -f "${TMP_DIR}/package/${BIN_NAME}" ]]; then
            cp "${TMP_DIR}/package/${BIN_NAME}" "${CACHE_DEST}"
          else
            echo "Could not find ${BIN_NAME} in tarball, listing contents:"
            find "${TMP_DIR}" -type f
            exit 1
          fi

          chmod +x "${CACHE_DEST}" 2>/dev/null || true
          echo "Pre-downloaded to ${CACHE_DEST}"
          ls -lh "${CACHE_DEST}"

      - name: Build binary
        if: steps.check.outputs.skip != 'true'
        uses: nick-fields/retry@v3
        with:
          timeout_minutes: 5
          max_attempts: 5
          retry_wait_seconds: 10
          shell: bash
          command: |
            PLATFORM="${{ matrix.platform }}"
            case "$PLATFORM" in
              darwin-arm64) TARGET="bun-darwin-arm64" ;;
              darwin-x64) TARGET="bun-darwin-x64" ;;
              darwin-x64-baseline) TARGET="bun-darwin-x64-baseline" ;;
              linux-x64) TARGET="bun-linux-x64" ;;
              linux-x64-baseline) TARGET="bun-linux-x64-baseline" ;;
              linux-arm64) TARGET="bun-linux-arm64" ;;
              linux-x64-musl) TARGET="bun-linux-x64-musl" ;;
              linux-x64-musl-baseline) TARGET="bun-linux-x64-musl-baseline" ;;
              linux-arm64-musl) TARGET="bun-linux-arm64-musl" ;;
              windows-x64) TARGET="bun-windows-x64" ;;
              windows-x64-baseline) TARGET="bun-windows-x64-baseline" ;;
            esac

            if [[ "$PLATFORM" == windows-* ]]; then
              OUTPUT="packages/${PLATFORM}/bin/oh-my-cortex.exe"
            else
              OUTPUT="packages/${PLATFORM}/bin/oh-my-cortex"
            fi

            bun build src/cli/index.ts --compile --minify --target=$TARGET --outfile=$OUTPUT

            echo "Built binary:"
            ls -lh "$OUTPUT"

      - name: Ad-hoc sign darwin binary
        if: steps.check.outputs.skip != 'true' && startsWith(matrix.platform, 'darwin-')
        run: |
          BINARY="packages/${{ matrix.platform }}/bin/oh-my-cortex"
          echo "Initial state:"
          codesign -dvvv "$BINARY" 2>&1 || true
          echo "Removing any existing signature:"
          codesign --remove-signature "$BINARY" 2>&1 || true
          echo "Applying ad-hoc signature:"
          codesign --sign - --force --timestamp=none "$BINARY"
          echo "Final state:"
          codesign -dvvv "$BINARY" 2>&1
          codesign --verify --verbose "$BINARY"

      - name: Compress binary
        if: steps.check.outputs.skip != 'true'
        run: |
          PLATFORM="${{ matrix.platform }}"
          cd packages/${PLATFORM}

          if [[ "$PLATFORM" == windows-* ]]; then
            7z a -tzip ../../binary-${PLATFORM}.zip bin/ package.json
          else
            tar -czvf ../../binary-${PLATFORM}.tar.gz bin/ package.json
          fi

          cd ../..
          echo "Compressed artifact:"
          ls -lh binary-${PLATFORM}.*

      - name: Upload artifact
        if: steps.check.outputs.skip != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.platform }}
          path: |
            binary-${{ matrix.platform }}.tar.gz
            binary-${{ matrix.platform }}.zip
          retention-days: 1
          if-no-files-found: error

  publish:
    needs: build
    if: always() && !cancelled()
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 1
      matrix:
        platform: [darwin-arm64, darwin-x64, darwin-x64-baseline, linux-x64, linux-x64-baseline, linux-arm64, linux-x64-musl, linux-x64-musl-baseline, linux-arm64-musl, windows-x64, windows-x64-baseline]
    steps:
      - name: Validate release inputs
        id: validate
        env:
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_DIST_TAG: ${{ inputs.dist_tag }}
          INPUT_PLATFORM: ${{ inputs.platform }}
        run: |
          VERSION="$INPUT_VERSION"
          DIST_TAG="$INPUT_DIST_TAG"
          PLATFORM="$INPUT_PLATFORM"

          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z]+(\.[0-9A-Za-z]+)*)?$ ]]; then
            echo "::error::Invalid version: $VERSION"
            exit 1
          fi

          if [ -n "$DIST_TAG" ] && ! [[ "$DIST_TAG" =~ ^[a-z][a-z0-9-]*$ ]]; then
            echo "::error::Invalid dist_tag: $DIST_TAG"
            exit 1
          fi

          case "$PLATFORM" in
            ""|darwin-arm64|darwin-x64|darwin-x64-baseline|linux-x64|linux-x64-baseline|linux-arm64|linux-x64-musl|linux-x64-musl-baseline|linux-arm64-musl|windows-x64|windows-x64-baseline) ;;
            *)
              echo "::error::Invalid platform: $PLATFORM"
              exit 1
              ;;
          esac

          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "dist_tag=$DIST_TAG" >> $GITHUB_OUTPUT
          echo "platform=$PLATFORM" >> $GITHUB_OUTPUT

      - name: Check if already published
        id: check
        env:
          VERSION: ${{ steps.validate.outputs.version }}
          TARGET_PLATFORM: ${{ steps.validate.outputs.platform }}
        run: |
          if [ -n "$TARGET_PLATFORM" ] && [ "$TARGET_PLATFORM" != "${{ matrix.platform }}" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Skipping oh-my-cortex-${{ matrix.platform }} because target platform is ${TARGET_PLATFORM}"
            exit 0
          fi

          STATUS=$(curl -s -o /dev/null -w "%{http_code}" "https://registry.npmjs.org/oh-my-cortex-${{ matrix.platform }}/${VERSION}")

          if [ "$STATUS" = "200" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "oh-my-cortex-${{ matrix.platform }}@${VERSION} already published"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Download artifact
        id: download
        if: steps.check.outputs.skip != 'true'
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: binary-${{ matrix.platform }}
          path: .

      - name: Extract artifact
        if: steps.check.outputs.skip != 'true' && steps.download.outcome == 'success'
        run: |
          PLATFORM="${{ matrix.platform }}"
          mkdir -p packages/${PLATFORM}

          if [[ "$PLATFORM" == windows-* ]]; then
            unzip binary-${PLATFORM}.zip -d packages/${PLATFORM}/
          else
            tar -xzvf binary-${PLATFORM}.tar.gz -C packages/${PLATFORM}/
          fi

          echo "Extracted contents:"
          ls -la packages/${PLATFORM}/
          ls -la packages/${PLATFORM}/bin/

      - uses: actions/setup-node@v4
        if: steps.check.outputs.skip != 'true' && steps.download.outcome == 'success'
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"

      - name: Publish oh-my-cortex-${{ matrix.platform }}
        if: steps.check.outputs.skip != 'true' && steps.download.outcome == 'success'
        env:
          DIST_TAG: ${{ steps.validate.outputs.dist_tag }}
          NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
          NPM_CONFIG_PROVENANCE: true
        run: |
          cd packages/${{ matrix.platform }}

          publish_args=(--access public --provenance)
          if [ -n "$DIST_TAG" ]; then
            publish_args+=(--tag "$DIST_TAG")
          fi

          waits=(900 1800 3600)

          for attempt in 1 2 3 4; do
            set +e
            npm publish "${publish_args[@]}" 2>&1 | tee npm-publish.log
            status=${PIPESTATUS[0]}
            set -e

            if [ "$status" -eq 0 ]; then
              exit 0
            fi

            if ! grep -Eiq "E429|Too Many Requests|rate limited" npm-publish.log; then
              exit "$status"
            fi

            if [ "$attempt" -eq 4 ]; then
              exit "$status"
            fi

            sleep_seconds=${waits[$((attempt - 1))]}
            echo "npm publish hit npm rate limiting for oh-my-cortex-${{ matrix.platform }} (attempt ${attempt}/4); retrying in ${sleep_seconds}s"
            sleep "$sleep_seconds"
          done
        timeout-minutes: 120
